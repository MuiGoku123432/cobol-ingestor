You are analyzing a COBOL source file to extract its structural skeleton. Extract ONLY what is explicitly present in the code — do not infer or fabricate.

File: {{.FileName}}

Return a single JSON object with these fields:

{
  "programId": "string — the PROGRAM-ID value",
  "executionMode": "string — BATCH, CICS, BATCH_AND_CICS, or UNKNOWN",
  "copyReferences": ["array of COPY member names (without extensions)"],
  "callTargets": [
    {
      "target": "string — called program name",
      "isDynamic": false
    }
  ],
  "paragraphs": ["array of paragraph names from PROCEDURE DIVISION"],
  "sections": ["array of section names from PROCEDURE DIVISION"],
  "fileDefinitions": [
    {
      "name": "string — FD name",
      "organization": "string — SEQUENTIAL, INDEXED, RELATIVE, or empty",
      "vsamType": "string — KSDS, ESDS, RRDS, or empty",
      "dataStoreType": "string — VSAM, DB2, IMS, or FLAT_FILE"
    }
  ],
  "dataItems": [
    {
      "name": "string — data item name",
      "level": 1,
      "picture": "string — PIC clause or empty",
      "usage": "string — COMP, COMP-3, BINARY, POINTER, DISPLAY, or empty"
    }
  ],
  "conditions": [
    {
      "name": "string — 88-level condition name",
      "parent": "string — parent data item name",
      "value": "string — condition value(s) (e.g., 'Y', 'SPACES', '1 THRU 5')"
    }
  ],
  "parameters": [
    {
      "name": "string — LINKAGE SECTION data item name",
      "level": 1,
      "direction": "string — IN, OUT, or INOUT (infer from usage if possible, default INOUT)"
    }
  ],
  "sqlStatements": [
    {
      "type": "string — SELECT, INSERT, UPDATE, DELETE, DECLARE, CALL, EXECUTE, etc.",
      "text": "string — first 200 chars of the SQL",
      "targetTable": "string — primary table name referenced in the statement, or empty",
      "hostVars": ["array of COBOL/Pro*C host variable names bound to this statement (e.g. :WS-CUST-ID without the colon prefix)"]
    }
  ],
  "cicsCommands": [
    {
      "command": "string — CICS command (SEND, RECEIVE, READ, WRITE, etc.)"
    }
  ],
  "externalInterfaces": [
    {
      "type": "string — MQ, CICS_LINK, CICS_XCTL, CICS_TS, CICS_TD, CICS_START, CICS_FILE, CICS_ENQ, IMS, IDMS, ADABAS, SORT, BATCH_UTIL, TCP, or FILE_TRANSFER",
      "details": "string — brief description (queue name, linked program, function code, PCB name, etc.)",
      "paragraph": "string — paragraph name where this interface is invoked, or empty"
    }
  ],
  "dbTables": [
    {
      "name": "string — table name (uppercase)",
      "schema": "string — schema/qualifier if present, else empty",
      "columns": ["array of column names referenced across all SQL for this table"],
      "operations": ["SELECT", "INSERT", "UPDATE", "DELETE"]
    }
  ],
  "idmsSchemas": [
    {
      "schemaName": "string — SCHEMA name from DB statement",
      "subschemaName": "string — subschema name from DB statement",
      "protocolMode": "string — PROTOCOL MODE (BATCH, IDMS-DC, or empty)"
    }
  ],
  "idmsRecords": [
    {
      "name": "string — IDMS record name",
      "area": "string — area name if identifiable, else empty"
    }
  ],
  "idmsAreas": [
    {
      "name": "string — IDMS area name",
      "usageMode": "string — RETRIEVAL, UPDATE, or empty"
    }
  ],
  "idmsSets": [
    {
      "name": "string — IDMS set name",
      "ownerRecord": "string — owner record name or empty",
      "memberRecord": "string — member record name or empty"
    }
  ]
}

Rules:
- For COPY references: extract the member name only (e.g., COPY CUSTFILE → "CUSTFILE")
- For CALL targets: CALL 'PROGNAME' → isDynamic=false; CALL WS-PROG-NAME → isDynamic=true
- For paragraphs: include only names from PROCEDURE DIVISION (not section headers)
- For data items: include 01-level and 77-level items only (skip subordinate levels except 88)
- For usage: extract the USAGE clause (COMP, COMP-3, BINARY, POINTER, DISPLAY); leave empty if not specified
- For conditions: extract ALL 88-level items with their parent data item and value(s)
- For parameters: extract data items from the LINKAGE SECTION; infer direction from how they are used (read-only = IN, written = OUT, both = INOUT)
- For SQL: extract from EXEC SQL ... END-EXEC blocks (both DB2 and Oracle); targetTable is the primary table (FROM, INTO, UPDATE, DELETE FROM). Pro*COBOL/Pro*C files use the same EXEC SQL syntax — treat identically
- For hostVars: list all COBOL data items or C variables used as bind variables in the SQL statement (prefixed with : in the source). Strip the colon. Examples: ":WS-CUST-ID" → "WS-CUST-ID", ":host_var" → "host_var". Only include host vars for this specific statement
- For CICS: extract from EXEC CICS ... END-EXEC blocks
- For executionMode: CICS if program contains EXEC CICS commands; BATCH if invoked only from JCL with no CICS; BATCH_AND_CICS if both patterns present; UNKNOWN if unclear
- For fileDefinitions: infer data store type from SELECT/ASSIGN clauses and file organization. INDEXED → VSAM KSDS, SEQUENTIAL with VSAM in ASSIGN → ESDS, RELATIVE → RRDS. If no VSAM indicators, classify as FLAT_FILE
- For externalInterfaces:
  - EXEC CICS LINK → CICS_LINK
  - EXEC CICS XCTL → CICS_XCTL
  - EXEC CICS WRITEQ TS / READQ TS / DELETEQ TS → CICS_TS (NOT MQ)
  - EXEC CICS WRITEQ TD / READQ TD → CICS_TD (NOT MQ)
  - EXEC CICS START TRANSID → CICS_START
  - CALL 'MQPUT' / 'MQGET' / 'MQOPEN' / 'MQCLOSE' / 'MQCONN' / 'MQDISC' / 'MQINQ' / 'MQSET' → MQ
  - CALL 'CBLTDLI' or 'AIBTDLI' → IMS. Details should include function code (GU, GN, GHU, GNP, ISRT, DLET, REPL) and PCB/PSB name if identifiable
  - CALL to socket libraries → TCP
  - EXEC CICS READ / WRITE / REWRITE / DELETE / STARTBR / READNEXT / READPREV / ENDBR → CICS_FILE. Details should include DATASET/FILE name and operation
  - EXEC CICS ENQ / DEQ → CICS_ENQ. Details should include RESOURCE name and length
  - BIND / READY / FINISH / OBTAIN / STORE / MODIFY / ERASE / FIND / GET in DML context (CA-IDMS) → IDMS. Details should include record name and area if identifiable
  - CALL 'ADABAS' or CALL 'ADACALL' → ADABAS. Details should include command code and file number if identifiable
  - SORT or MERGE verb → SORT. Details should include sort file name and INPUT/OUTPUT PROCEDURE or USING/GIVING clause
  - CALL 'DFSORT' / 'ICETOOL' / 'IDCAMS' / 'IEBGENER' / 'IEBCOPY' → BATCH_UTIL. Details should include utility name and operation
  - Always populate the paragraph field with the paragraph name where the interface is invoked
- For dbTables: deduplicate across all SQL statements. Collect all column names and operations per table. Extract table names from FROM, JOIN, INTO, UPDATE, DELETE FROM clauses. Extract column names from SELECT lists, WHERE predicates, SET clauses, INSERT column lists. Only include tables from EXEC SQL blocks
- For COPY IDMS: COPY IDMS SUBSCHEMA-CTRL → copyReference is "SUBSCHEMA-CTRL"; COPY IDMS RECORD MANUAL → copyReference is "MANUAL". The actual member name follows the IDMS keyword (and optional RECORD/MAP/MODULE qualifier)
- For IDMS SCHEMA SECTION: extract DB subschema WITHIN schema → idmsSchemas entry. Protocol mode from IDMS-CONTROL SECTION (PROTOCOL. MODE IS xxx)
- For IDMS records: extract record names from COPY IDMS RECORD statements, OBTAIN/FIND/STORE/MODIFY/ERASE DML verbs, and BIND RECORD statements
- For IDMS areas: extract area names from READY statements (READY areaName USAGE-MODE xxx)
- For IDMS sets: extract set names from CONNECT/DISCONNECT statements and OBTAIN/FIND WITHIN SET navigation
- For executionMode: add IDMS_DC (program runs under IDMS DC/UCF) and IDMS_BATCH (program uses IDMS in batch mode) values. Detect from PROTOCOL MODE or IDMS-CONTROL section
- For errorHandling pattern: add IDMS-STATUS value for programs that check IDMS error status codes (DB-STATUS-CODE, ERROR-STATUS, IDMS-STATUS)
- Return ONLY the JSON object, no markdown fences, no explanation
{{if .FewShot}}
Here is an example of the expected input/output format:
{{.FewShot}}

Now analyze the following file:
{{end}}